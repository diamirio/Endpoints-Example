# Endpoints Example

This is an example iOS application demonstrating the [Endpoints](https://github.com/diamirio/Endpoints) framework - a type-safe, declarative way to define and consume HTTP endpoints in Swift.
